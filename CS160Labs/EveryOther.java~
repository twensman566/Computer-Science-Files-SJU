public class EveryOther
{
 
  public int everyOther( int [] array, int i)
  {
    int sum = 0;
   if(i== array.length)
   {
    return sum;
   }
   else
   {
    sum = sum + everyOther(array, i+2);
    
   }
   return sum;
  }
}